// Custom styles.

.table-container {
  border-radius: 6px;
}

.table-container table {
  border-collapse:separate;
  border-spacing: 0 4px;
  padding: 0 6px 2px 6px;

  if (hexo-config('darkmode')) {
    @media (prefers-color-scheme: dark) {
     background-color: #555;
    }
}
  background-color: #eee;
}

.table-container th, td {
  border: 0;
}

.table-container tr td:first-child {
  border-radius: 4px 0 0 4px;
}

.table-container tr td:last-child {
  border-radius: 0 4px 4px 0;
}

body {
  background-image: url("https://raw.githubusercontent.com/HandyOrg/HandyOrgResource/master/HandyControl/Resources/cloud.png");
  background-repeat: repeat; /* Tiling enabled */
  background-position: top left; /* Start tiling from the top-left corner */
  background-attachment: fixed; /* Keeps the background static during scrolling */
}